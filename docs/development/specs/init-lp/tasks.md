# Implementation Plan

- [x] 1. 基本レイアウト（背景/ヘッダー/フッター）を組み立てる
- [x] 1.1 全画面背景に `bg.png` を適用し、ファーストビューを1画面で成立させる
  - 背景がスクロールやリサイズで破綻しないようにする
  - 主要要素（ヘッダー/中央/フッター）が背景上で視認できるようにする
  - _Requirements: 1.1, 4.1_
- [x] 1.2 (P) ヘッダーに `marry.fun` 表記を配置する
  - レスポンシブで崩れない配置にする
  - _Requirements: 5.1, 1.3_
- [x] 1.3 (P) フッターに `Coming soon...` と `X/Help/Docs` のリンク領域を配置する
  - URL未確定時は「準備中」等の無効状態表現を含める
  - _Requirements: 5.2, 5.3, 5.4_

- [x] 2. ロゴを中央に表示し、ふわっと登場アニメーションを実装する
- [x] 2.1 ロゴを中央付近に表示し、視認性を担保する
  - 背景とのコントラストを確保し、ロゴが常に判別できる状態にする
  - _Requirements: 1.2, 4.3_
- [x] 2.2 アニメーション（フェード/浮遊感）でロゴを登場させる
  - 初回表示でカクつきが目立たないようにする
  - _Requirements: 3.1, 3.2_
- [x] 2.3 `prefers-reduced-motion` 有効時のアニメ抑制を適用する
  - 動きの代替としてフェードのみ、または即時表示に切り替える
  - _Requirements: 3.3_

- [x] 3. ギャルゲー風の背景演出・デザイン調整を行う
- [x] 3.1 背景表現（パステル/ポップ/キラキラ感）を追加し、世界観を作る
  - 装飾が主要要素の視認性を阻害しないように調整する
  - _Requirements: 4.2, 4.4_

- [x] 4. BGM（use-sound）ループ再生を実装する
- [x] 4.1 BGMをループ再生し、再生状態をUIに反映する
  - 初回訪問時に再生開始を試み、ループ再生を維持する
  - 再生中/停止中が分かる表示を用意する
  - _Requirements: 2.1, 2.2, 2.4_
- [x] 4.2 自動再生制限に対する導線（CTA）を実装する
  - 自動再生失敗時にユーザー操作で再生開始できるようにする
  - _Requirements: 2.3_

- [x] 5. 3Dモデル（R3F）を中央に表示する
- [x] 5.1 `@react-three/fiber`（R3F）で3Dシーンを描画できる状態にする
  - 必要な依存関係を導入し、ページ内のCanvas描画を成立させる
  - _Requirements: 7.1_
- [x] 5.2 `girl.glb` を読み込み、ファーストビュー中央付近に配置する
  - カメラ/ライトを調整し、モデルが見える状態にする
  - _Requirements: 7.2_
- [x] 5.3 読み込み中/失敗時のフォールバック表示を実装する
  - ローディングやエラーメッセージを表示し、LP全体の体験を破綻させない
  - _Requirements: 7.3, 6.2_
- [x] 5.4 `prefers-reduced-motion` 有効時に3Dの自動アニメーションを抑制する
  - 常時回転などがある場合は無効化/抑制する
  - _Requirements: 7.4_

- [x] 6. 互換性・品質の最低限チェックを行う
- [x] 6.1 主要ブラウザで表示が成立し、重大なコンソールエラーが無い状態にする
  - Chrome/Safari/Firefox相当の挙動差に配慮して仕上げる
  - _Requirements: 6.1, 6.3_
- [ ]\* 6.2 (P) テストを追加して主要状態遷移を検証する（必要に応じて）
  - BGM: 自動再生ブロック→CTA→再生開始
  - 3D: ロード成功/失敗でUIが破綻しない
  - _Requirements: 2.3, 6.2, 7.3_
